@model GOTHelperEng.Models.Point

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Point</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="PointId" />
            <div class="form-group">
                <label asp-for="PointName" class="control-label"></label>
                <input asp-for="PointName" class="form-control" />
            </div>
                <span asp-validation-for="PointName" class="text-danger"></span>
            
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a class="btn btn-primary" asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
