@model GOTHelperEng.Models.Gender

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Gender</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.GenderName)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.GenderName)
        </dd>
    </dl>
</div>
<div>
    @if (User.IsInRole("Admin"))
    {
        <a class="btn btn-primary" asp-action="Edit" asp-route-id="@Model?.GenderId">Edit</a> <text>|</text>
    }
    <a class="btn btn-primary" asp-action="Index">Back to List</a>
</div>
