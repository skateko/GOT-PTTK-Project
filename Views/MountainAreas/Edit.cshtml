@model GOTHelperEng.Models.MountainArea

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>MountainArea</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="MountainAreaId" />
            <div class="form-group">
                <label asp-for="MoutainAreaName" class="control-label"></label>
                <input asp-for="MoutainAreaName" class="form-control" />
            </div>
                            <span asp-validation-for="MoutainAreaName" class="text-danger"></span>

            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a class="btn btn-primary" asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
